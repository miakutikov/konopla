# 📘 Konopla.UA — Повна документація

Цей документ — все що потрібно знати для керування порталом.

---

## 1. Як все працює (коротко)

Кожні 6 годин GitHub автоматично:
1. Збирає новини з 28 джерел (RSS фіди)
2. Фільтрує наркотичний контент
3. Переписує новини українською через AI (Gemini)
4. Підбирає фото (Unsplash)
5. Публікує на сайт + в Telegram + генерує Instagram Stories
6. Якщо щось зламалось — надсилає тобі алерт у Telegram

Ти не робиш нічого. Система працює сама.

---

## 2. Щоденне керування

### 2.1 Перевірити що все працює
- Відкрий вкладку **Actions** на GitHub
- Останній запуск має мати зелену галочку ✅
- Якщо червоний хрестик ❌ — натисни на нього і подивись помилку

### 2.2 Видалити погану новину
1. Зайди в папку `content/news/` на GitHub
2. Знайди файл новини (вони називаються по даті і заголовку)
3. Натисни на файл → іконка кошика → Commit changes
4. Через 5 хвилин новина зникне з сайту

### 2.3 Ручний запуск
Actions → "Konopla.UA News Pipeline" → "Run workflow" → "Run workflow"

---

## 3. Налаштування

### 3.1 Змінити частоту публікації
Файл: `.github/workflows/pipeline.yml`
Рядок: `cron: '0 */6 * * *'`

| Розклад | Що означає |
|---|---|
| `0 */6 * * *` | Кожні 6 годин (за замовчуванням) |
| `0 */4 * * *` | Кожні 4 години |
| `0 */3 * * *` | Кожні 3 години |
| `0 8,14,20 * * *` | Три рази на день: 8:00, 14:00, 20:00 UTC |
| `0 6,12,18,0 * * *` | Чотири рази на день |

⚠️ Час вказано в UTC. Україна = UTC+2 (зима) або UTC+3 (літо).

### 3.2 Змінити кількість новин за запуск
Файл: `scripts/config.py`
Параметр: `MAX_ARTICLES_PER_RUN = 8`
Збільш до 12-15 якщо хочеш більше контенту.

### 3.3 Додати нове RSS джерело
Файл: `scripts/config.py`
Масив: `RSS_FEEDS`
Додай URL в кінець списку, наприклад:
```python
"https://news.google.com/rss/search?q=hemp+ukraine&hl=uk&gl=UA&ceid=UA:uk",
```

### 3.4 Підключити Google Analytics
1. Зайди на analytics.google.com
2. Створи акаунт і ресурс для konopla.ua
3. Отримай Measurement ID (виглядає як `G-XXXXXXXXXX`)
4. Відкрий файл `hugo.toml`
5. Знайди рядок `# googleAnalytics = "G-XXXXXXXXXX"`
6. Прибери # і встав свій ID:
```
googleAnalytics = "G-ABC123XYZ"
```

### 3.5 Змінити дизайн / кольори
Файл: `static/css/style.css`
Всі кольори зібрані у CSS змінних на початку файлу:
```css
:root {
  --green-900: #1b4332;   /* найтемніший зелений */
  --green-800: #2d6a4f;   /* основний зелений */
  --green-600: #52b788;   /* акцентний зелений */
  ...
}
```
Зміни значення → commit → сайт оновиться.

### 3.6 Змінити Telegram канал
1. GitHub → Settings → Secrets → `TELEGRAM_CHAT_ID` → Update
2. Файл `hugo.toml` → `telegram = "https://t.me/НОВИЙ_КАНАЛ"`

---

## 4. Рекламні зони

На сайті є 4 готові місця для реклами:

| Позиція | Де показується | Рекомендований розмір |
|---|---|---|
| `top` | Вгорі головної сторінки | 728×90 (Leaderboard) |
| `in-feed` | Між новинами в стрічці | 336×280 (Large Rectangle) |
| `sidebar` | Бічна панель статті | 300×250 (Medium Rectangle) |
| `article-bottom` | Під текстом статті | 728×90 |

### Як розмістити рекламу
Файл: `layouts/partials/ad-banner.html`

Замість плейсхолдера вставь HTML рекламодавця. Наприклад для позиції "top":
```html
{{ if eq $position "top" }}
<div class="ad-zone ad-zone-top">
  <a href="https://рекламодавець.com" target="_blank">
    <img src="/images/ads/banner-top.jpg" alt="Реклама">
  </a>
</div>
{{ end }}
```

Або для Google AdSense — вставь код від Google замість плейсхолдера.

---

## 5. Instagram Stories

Система автоматично генерує картинки 1080×1920 для кожної новини.

### Де знаходяться
Папка: `static/instagram/` (в репозиторії)
Файли: `story-YYYYMMDD-HHMM-заголовок.png`

### Як постити
**Варіант 1 — вручну (безкоштовно):**
1. Зайди в `static/instagram/` на GitHub
2. Скачай нові картинки
3. Опублікуй через Instagram

**Варіант 2 — через сервіс (платно):**
Later.com або Buffer.com ($15/міс) — можна підключити автоматичний постинг.

### Як виглядає Story
- Зелений градієнт фон
- Логотип Konopla.UA зверху
- Категорія з емоджі
- Великий заголовок по центру
- Короткий підпис
- CTA "Читати на konopla.ua" внизу

---

## 6. Моніторинг

Система автоматично відправляє алерти в Telegram якщо:
- ❌ Pipeline впав повністю → `CRITICAL` алерт
- ⚠️ Більше половини новин не вдалось обробити → `WARN` алерт  
- ℹ️ Нових новин не знайдено → `INFO` (тихе, без звуку)

### Налаштувати персональні алерти (не в канал)
Додай в GitHub Secrets:
- `ADMIN_CHAT_ID` = твій особистий Telegram ID

Щоб дізнатись свій ID: напиши боту @getmyid_bot у Telegram.

Тоді алерти приходитимуть тобі в особисті повідомлення, а не в канал.

---

## 7. Що робити якщо щось зламалось

### Pipeline показує ❌ в Actions

**Причина: "GEMINI_API_KEY not set"**
→ Перевір Secrets: Settings → Secrets → GEMINI_API_KEY

**Причина: "Rate limited" або "429 error"**
→ Gemini безкоштовний tier перевищено. Зменш `MAX_ARTICLES_PER_RUN` в config.py або збільш `API_DELAY_SECONDS`.

**Причина: "No new articles found"**
→ Це нормально! Означає що за останні 6 годин нових новин не було.

**Причина: "Hugo build failed"**
→ Можливо зламаний markdown файл. Перевір останні файли в `content/news/` — видали проблемний.

### Сайт не відкривається

1. Перевір Settings → Pages — чи ввімкнено GitHub Pages
2. Перевір DNS записи домену
3. Зачекай 10-30 хвилин після зміни DNS

### Telegram бот не постить

1. Перевір що бот є адміністратором каналу
2. Перевір Secrets: TELEGRAM_TOKEN і TELEGRAM_CHAT_ID
3. Перевір що Chat ID правильний (з @)

---

## 8. Секрети GitHub (повний список)

| Секрет | Обов'язковий | Що це |
|---|---|---|
| `GEMINI_API_KEY` | ✅ Так | Ключ API від aistudio.google.dev |
| `TELEGRAM_TOKEN` | ✅ Так | Токен бота від @BotFather |
| `TELEGRAM_CHAT_ID` | ✅ Так | ID каналу (@username або -100xxx) |
| `UNSPLASH_ACCESS_KEY` | Ні | Ключ для фото (unsplash.com/developers) |
| `ADMIN_CHAT_ID` | Ні | Твій Telegram ID для персональних алертів |

---

## 9. Структура файлів

```
konopla-ua/
├── .github/workflows/
│   └── pipeline.yml          ← Розклад + pipeline
├── content/
│   ├── about.md              ← Сторінка "Про нас"
│   └── news/
│       ├── _index.md
│       └── *.md              ← Новини (створюються автоматично)
├── data/
│   └── processed.json        ← Список оброблених (не чіпай)
├── layouts/
│   ├── _default/
│   │   ├── baseof.html       ← Головний каркас
│   │   ├── single.html       ← Сторінка статті
│   │   ├── list.html         ← Список новин / категорія
│   │   └── about.html        ← Layout для "Про нас"
│   ├── index.html            ← Головна сторінка
│   └── partials/
│       ├── seo-head.html     ← SEO мета-теги
│       ├── analytics.html    ← Google Analytics
│       ├── header.html       ← Шапка + навігація
│       ├── footer.html       ← Підвал
│       ├── article-card.html ← Картка новини
│       └── ad-banner.html    ← Рекламні зони
├── scripts/
│   ├── config.py             ← Все налаштування
│   ├── fetcher.py            ← Збирає RSS
│   ├── rewriter.py           ← Gemini рерайт
│   ├── publisher.py          ← Генерує markdown
│   ├── images.py             ← Unsplash фото
│   ├── instagram.py          ← Instagram Stories
│   ├── telegram_bot.py       ← Постинг в Telegram
│   ├── monitor.py            ← Алерти
│   └── main.py               ← Головний скрипт
├── static/
│   ├── css/style.css         ← Стилі сайту
│   ├── images/favicon.svg    ← Іконка
│   ├── instagram/            ← Згенеровані Stories
│   └── robots.txt            ← Для пошукових систем
├── hugo.toml                 ← Конфіг Hugo
├── README.md                 ← Інструкція з розгортання
└── PROGRESS.md               ← Цей файл
```

---

## 10. Розвиток проєкту (що можна додати потім)

- **Більше джерел** — додавай RSS з конференцій, Reddit, LinkedIn
- **Мультимовність** — Hugo підтримує кілька мов, можна додати EN версію
- **Коментарі** — підключити Disqus або Telegram коментарі
- **Email розсилка** — підключити Mailchimp або Buttondown
- **Пошук по сайту** — Algolia або Pagefind (безкоштовний)
- **Більше соцмереж** — Facebook автопост через API, Twitter/X
- **Premium контент** — аналітичні огляди, написані вручну
